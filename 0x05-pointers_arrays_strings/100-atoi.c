#include "main.h"

/**
 * _atoi - Entry point
 * @s: input
 * Return: Always 0 (Success)
 */
int _atoi(char *s)
{
  int i = 0;
	while (true)
	{
	  if (s[i] != '+' && s[i] != '-' && s[i] != '0' && s[i] != '1' && s[i] != '2' && s[i] != '3' && s[i] != '4' && s[i] != '5' && s[i] != '6' && s[i] != '7' && s[i] != '8' && s[i] != '9')
	    {
	      continue;
	      else
		{
		  if (s[i] = '-')
		    {
		      ++minu;
		    }
		  else if (s[i] = '+')
		    {
		      continue;
		    }
		  else
		    
}
